#R PROGRAMING
# Bu kod raster verisinden fwi değerlerini hesaplamaktadır.

#library(cffdrs)
#require(terra)

rasters <- rast("rasterfwi_lidia.tif")

# assign variable names:
names(rasters) <- c("temp", "rh", "ws", "prec")

# (1) use the initial values
fwi_rasters <- fwiRaster(rasters, 
init = c(ffmc = 88.64, dmc = 176.2, dc = 277.13))

writeRaster(fwi_rasters$FWI, "fwi_result_lidia.tif", overwrite = TRUE)

#plot(fwi_rasters$FWI)

#Palisides SE030 c(87.9,169.9,359.1)